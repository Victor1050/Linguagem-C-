#include <iostream>
#include <cstdlib>

using namespace std;

int main()
{
	
	int vA[3];
	
	/* carregar vetor*/
	for(int i=0;i<3;i++)
	{
		cout<<("\n informe idade: ");
		cin>>(vA[i]);
	}
		/* descarregar vetor*/
	for(int i=0;i<3;i++)
	{
		cout<<("\n idade informada: ");
		cout<<(vA[i]);
	}

	
};
